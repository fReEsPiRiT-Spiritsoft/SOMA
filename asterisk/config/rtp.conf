; ================================================================
; RTP (Real-time Transport Protocol) Config
; Audio streams for SIP calls use RTP.
; ================================================================
[general]
rtpstart = 20000     ; RTP port range start (nicht 10000 — manche Router blocken das)
rtpend   = 20100     ; RTP port range end
icesupport = no
stunaddr =

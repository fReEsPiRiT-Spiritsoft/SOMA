; ================================================================
; Asterisk HTTP Server — Needed for ARI (REST Interface)
; SOMA connects to ws://localhost:8088/ari/events
; ================================================================
[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
; prefix = asterisk   ; optional URL prefix

; ================================================================
; Asterisk Main Config — SOMA Phone Gateway
; ================================================================
[options]
verbose = 3
debug = 0
runuser = asterisk
rungroup = asterisk

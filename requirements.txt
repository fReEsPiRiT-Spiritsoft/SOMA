# ============================================================================
# SOMA-AI – Python-Abhängigkeiten
# Installieren mit: pip install -r requirements.txt
# (wird automatisch von start_soma.sh in einem .venv erledigt)
# ============================================================================

# ── Core API (FastAPI + ASGI-Server) ─────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.34        # [standard] bringt uvloop, websockets, httptools
starlette>=0.41

# ── HTTP-Clients ─────────────────────────────────────────────────────────
httpx>=0.28
aiohttp>=3.11

# ── Datenvalidierung ─────────────────────────────────────────────────────
pydantic>=2.10
pydantic-settings>=2.7

# ── Datenbank / Cache ────────────────────────────────────────────────────
redis[asyncio]>=5.2
psycopg2-binary>=2.9           # Django → PostgreSQL

# ── Django (Memory-UI / SSOT) ────────────────────────────────────────────
django>=5.1
python-dotenv>=1.0

# ── System-Monitoring ────────────────────────────────────────────────────
psutil>=7.0

# ── Logging ──────────────────────────────────────────────────────────────
structlog>=25.0

# ── MQTT (Discovery & Home-Assistant-Bridge) ─────────────────────────────
aiomqtt>=2.5
paho-mqtt>=2.1                 # Basis für aiomqtt

# ── Audio / Sprache ──────────────────────────────────────────────────────
numpy>=2.0
webrtcvad-wheels>=2.0          # VAD (Voice Activity Detection)
faster-whisper>=1.2            # Lokale STT (Speech-to-Text)
soundfile>=0.13                # Audio-Datei I/O
librosa>=0.11                  # Audio-Analyse (Stimmung, Pitch)
audioop-lts>=0.2               # Python 3.13+ Kompatibilität für Audioop

# ── TTS (Text-to-Speech) ─────────────────────────────────────────────────
piper-tts>=1.4                 # Lokale TTS-Engine

# ── Web-Suche (WebSearch-Modul) ───────────────────────────────────────────
# httpx ist bereits oben – keine weiteren Abhängigkeiten nötig

# ── Hilfsbibliotheken ────────────────────────────────────────────────────
rich>=14.0                     # Schöne Terminal-Ausgaben
requests>=2.32                 # Sync HTTP (Setup-Checks)
